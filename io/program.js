/*global require, process */

(function() {

    'use strict';

    process.stdin.resume();
    var util = require('util');

    process.stdin.on('data', function (text) {
    }).pipe(process.stdout);
    
})();
